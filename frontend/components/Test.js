// Just for testing the navigation bar on Barn Page

import * as React from 'react';
import { Text, View, TextInput, Image, ScrollView, Keyboard, TouchableOpacity } from 'react-native';

export default function Test({navigation}) {
    return (
        <View>
            <Text style={{top: 70, marginLeft: 50}}>Test</Text>
        </View>
    )
}